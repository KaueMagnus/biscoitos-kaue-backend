meta {
  name: editar-produto
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/produtos/{id}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
