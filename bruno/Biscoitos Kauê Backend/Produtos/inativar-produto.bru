meta {
  name: inativar-produto
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/produtos/2/inativar
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
