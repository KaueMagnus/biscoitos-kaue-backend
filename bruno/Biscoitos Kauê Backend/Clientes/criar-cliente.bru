meta {
  name: criar-cliente
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/clientes
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
