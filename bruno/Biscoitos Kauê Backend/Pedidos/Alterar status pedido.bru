meta {
  name: Alterar status pedido
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/pedidos/1/status
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
